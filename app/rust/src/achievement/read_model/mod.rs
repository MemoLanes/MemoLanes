//! Read-models: join computed state (from [`compute`](super::compute)) with the
//! worldview geo tree into the list / progress / detail shapes the `api/achievement`
//! FRB layer serves.
pub mod region;
